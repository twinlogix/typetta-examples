schema: 'src/schema.graphql'
generates:
  src/models.ts:
    plugins:
      - 'typescript'
    config:
      scalars:
        Date: Date
  src/dao.ts:
    config:
      schemaImport: './schema.graphql'
      tsTypesImport: './models'
    plugins:
      - '@twinlogix/typetta'
